import { TEXT_BLUR_COLOR } from '@/styles/colorPalette';

const MuiLabel = {
    styleOverrides: {
        outlined: {
            transform: 'translate(14px, 17px) scale(1)',
            color: TEXT_BLUR_COLOR,
        },
    },
};

export default MuiLabel;
